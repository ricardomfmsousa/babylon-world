# Typescript 3D world explorer using Babylon.js

## Development

- Starts @ [http://localhost:8080](http://localhost:8080)

  ```
  yarn && yarn start
  ```

## Build

- Builds to `dist` folder

  ```
  npm run build
  ```
